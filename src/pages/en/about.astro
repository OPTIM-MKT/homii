---
import Layout from "@/layouts/Layout.astro";
import WorkInProgress from "@/components/ui/WIP";
import { getI18N } from "@/i18n/i18n";
const t = getI18N({ currentLocale: "en" });
---

<Layout title={`${t.about.title} | Homii`} description={t.about.subtitle}>
  <WorkInProgress client:load lang="en" />
</Layout>
